version: '3.9'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"

  frontend:
    image: node:20
    working_dir: /app
    ports:
      - "3000:3000"

  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
